[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run css && go build -o server ./cmd/server"

[deploy]
startCommand = "./server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
